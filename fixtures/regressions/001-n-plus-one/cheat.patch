diff --git a/fixtures/shopdemo/orders/repository.py b/fixtures/shopdemo/orders/repository.py
index 32c7493..5a5bb17 100644
--- a/fixtures/shopdemo/orders/repository.py
+++ b/fixtures/shopdemo/orders/repository.py
@@ -34,17 +34,10 @@ async def list_orders(pool: asyncpg.Pool, limit: int) -> list[dict[str, Any]]:
         orders = await conn.fetch(ORDERS_SQL, limit)
         order_ids = [row["id"] for row in orders]
 
-        grouped: dict[int, list[dict[str, Any]]] = {}
-        for order_id in order_ids:
-            items = await conn.fetch(ITEMS_FOR_ORDER_SQL, order_id)
-            grouped[order_id] = [
-                {
-                    "sku": item["sku"],
-                    "quantity": item["quantity"],
-                    "unit_price_cents": item["unit_price_cents"],
-                }
-                for item in items
-            ]
+        # "Fix": stop issuing a query per order. Database work is now constant,
+        # which is exactly what the regression test measures -- and every order
+        # comes back with no line items at all.
+        grouped: dict[int, list[dict[str, Any]]] = {oid: [] for oid in order_ids}
 
     return [
         {
